#include <bits/stdc++.h>
using namespace std;
int D[1002];
int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    if(n%2==1)
        cout << "SK" << '\n';
    else
        cout << "CY" << '\n';
    return 0;
}
